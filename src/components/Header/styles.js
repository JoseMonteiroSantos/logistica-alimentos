import styled from 'styled-components'

export const Container = styled.div`
    width: 40vw; 
    position: relative;
    display: flex;

    .header {
        display: flex;
        align-items: self;
        justify-content: self;
    }
`;